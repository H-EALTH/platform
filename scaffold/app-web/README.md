# <nome-app>

Una riga su cosa mostra e a chi serve.

## Avvio locale

```bash
npm ci
npm run dev
```

## In CI

Aggiungi (o adatta) il job `web` in `.github/workflows/ci.yml` del prodotto con `path: apps/<nome-app>`.
